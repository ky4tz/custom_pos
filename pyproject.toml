[project]
name = "custom_pos"
authors = [
    { name = "ky4tz", email = "ky4tz@icloud.com"}
]
description = "Adds custom fields to POS"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
